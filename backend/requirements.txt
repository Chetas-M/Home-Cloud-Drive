# Home Cloud Backend Dependencies
fastapi==0.136.1
uvicorn[standard]==0.27.0
python-multipart==0.0.27
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
sqlalchemy==2.0.36
aiosqlite==0.19.0
pydantic>=2.9.0
pydantic-settings>=2.5.0
aiofiles==23.2.1
email-validator==2.1.0
Pillow==12.2.0
slowapi==0.1.9

# -------------------------------------------------------------------
# Rich Search Indexing (optional — install for richer content search)
# All packages below degrade gracefully when absent.
# -------------------------------------------------------------------

# PDF extraction (try pdfminer first; pypdf as fallback)
pdfminer.six>=20221105
pypdf>=4.0.0

# Office document extraction
python-docx>=1.1.0
openpyxl>=3.1.0
python-pptx>=0.6.23

# Legacy Excel (.xls) support
xlrd>=2.0.1

# Image OCR (also requires Tesseract binary: apt install tesseract-ocr)
# Uncomment to enable:
# pytesseract>=0.3.10
